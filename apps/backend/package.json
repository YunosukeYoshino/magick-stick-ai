{
	"name": "@character-sheet-ai-assistant/backend",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"typecheck": "bunx tsc --noEmit",
		"build": "bunx tsc"
	},
	"dependencies": {
		"@google/genai": "^1.19.0",
		"hono": "^4.9.7"
	},
	"devDependencies": {
		"typescript": "^5.8.0",
		"wrangler": "^4.4.0"
	}
}
